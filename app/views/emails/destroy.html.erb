<h1>Email for <%= Contact.find(@email.contact_id).name %> deleted</h1>

<p><a href="/contacts">See all the contacts</a></p>
<p><a href="/contacts/new">Create a new contact</a></p>
<p><a href="/contacts/<%= @email.contact_id %>">View details for <%= Contact.find(@email.contact_id).name %></a></p>
<p><a href="/contacts/<%= @email.contact_id %>/emails/new">Create a new email for <%= Contact.find(@email.contact_id).name %></a></p>
